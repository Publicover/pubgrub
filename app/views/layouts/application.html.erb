<!DOCTYPE html>
<html>
  <head>
    <title>Pubgrub</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% unless notice.nil? %>
      <p class="notice middle"><%= notice %></p>
    <% end %>
    <% unless alert.nil? %>
      <p class="alert middle"><%= alert %></p>
    <% end %>

    <%= render 'partials/header' unless request.env['PATH_INFO'] == '/users/sign_in' || request.env['PATH_INFO'] == '/users/sign_up'%>
    <%= render 'partials/page_heading' unless request.env['PATH_INFO'] == '/' %>
    <div class='container'>
      <%= yield %>
    </div>
  </body>
</html>
