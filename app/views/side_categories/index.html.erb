<table>
  <thead>
    <tr>
      <th>
        Name
      </th>
    </tr>
  </thead>
  <tbody>
    <% @side_categories.each do |category| %>
      <tr>
        <td>
          <%= link_to "#{category.name}", side_category_path(category) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
