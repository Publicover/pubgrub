<div class='nested-fields'>
  <%= f.hidden_field :_destroy %>
  <%= f.hidden_field :ingredientable_id, value: @entree.id %>
  <%= f.hidden_field :ingredientable_type, value: 'Entree' %>

  <hr />
  <p>
    <%= f.label 'Grocery' %>
    <%= f.collection_select :grocery_id, Grocery.all.order(name: :asc), :name, :id, prompt: true %>
  </p>

  <p>
    <%= f.label 'Measurement' %>
    <%= f.collection_select :measurement, Measurement.all, :measure, :measure, prompt: true %>
  </p>

  <p>
    <%= f.label 'Quantity' %>
    <%= f.number_field :quantity, step: 0.01 %>
  </p>

  <p>
    <%= link_to 'Remove', '#', class: "remove_fields" %>
  </p>
  <hr />
</div>
